export declare class ProductDTO {
    productId: string;
    title: string;
    quantity: number;
    unitPrice: number;
    currencyId: string;
    constructor(productId: string, title: string, quantity: number, unitPrice: number, currencyId: string);
}
